[test]
# Use happy-dom for DOM testing
preload = ["./test-setup.ts"]

# Exclude e2e tests (they use Playwright, not Bun)
exclude = [
  "e2e/**/*",
  "node_modules/**/*",
  "dist/**/*",
  ".next/**/*"
]

