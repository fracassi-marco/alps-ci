# Alps-CI Environment Variables

# Node Environment
NODE_ENV=development

# Server Port (default: 3000)
# PORT=3000

# Database Configuration
# For local development (Docker Compose):
DATABASE_URL=postgresql://alpsci:alpsci_dev_password@localhost:5432/alpsci

# For production (use your managed PostgreSQL service):
# DATABASE_URL=postgresql://user:password@host:5432/database

# Authentication Configuration
BETTER_AUTH_SECRET=your-secret-key-at-least-32-characters-long
BETTER_AUTH_URL=http://localhost:3000
NEXT_PUBLIC_BETTER_AUTH_URL=http://localhost:3000

# Google OAuth Configuration
GOOGLE_CLIENT_ID=your-google-client-id
GOOGLE_CLIENT_SECRET=your-google-client-secret

# Note: Personal Access Tokens are stored encrypted in the database (builds table)
# Generate BETTER_AUTH_SECRET with: openssl rand -base64 32 or bun run auth:generate-secret

